diff --git a/cpp/calc.cpp b/cpp/calc.cpp
index 09255ab..93d32d4 100644
--- a/cpp/calc.cpp
+++ b/cpp/calc.cpp
@@ -6,7 +6,6 @@ extern "C" double calc_divide(int a, int b) {
         //   native -> SIGILL (crash)   |   wasm -> trap (catchable in Chicory)
         __builtin_trap();
     }
-    // v0.1: integer division, then widened to the frozen double contract.
-    // The live demo changes ONLY this line to: return (double)a / (double)b;
-    return (double)(a / b);
+    // v0.2 (live demo): real float division on the same frozen double contract.
+    return (double)a / (double)b;
 }
diff --git a/webassembly/VERSION b/webassembly/VERSION
index 49d5957..3b04cfb 100644
--- a/webassembly/VERSION
+++ b/webassembly/VERSION
@@ -1 +1 @@
-0.1
+0.2
